    <div class="charity-top-bar" style="background:var(--blue-700);color:white;text-align:center;padding:6px 16px;font-size:0.8rem;">
        A portion of every payment supports#x1F49A; A portion of every payment supports <a href="/charity" style="color:var(--yellow-300);font-weight:700;text-decoration:underline;"><%= charityName %></a>
    </div>

    <% if (user && user.verified !== undefined && !user.verified) { %>
    <% } %>
    <% if (user && user.role !== 'owner' && user.role !== 'tutor') { %>
    <% var _payStatus = (typeof paymentStatus !== 'undefined') ? paymentStatus : null; %>
    <% if (_payStatus && _payStatus !== 'paid') { %>
    <div style="background:var(--red-500);color:white;text-align:center;padding:10px 16px;font-size:0.9rem;font-weight:600;">
        Payment is due on your account. <a href="/payment/pay" style="color:white;text-decoration:underline;font-weight:700;">Make a payment now</a>
    </div>
    <% } %>
    <% } %>

    <nav class="nav" role="navigation" aria-label="Main navigation">
        <div class="nav-inner">
            <a href="/" class="nav-logo" aria-label="<%= siteName %> Home">
                <img src="/uploads/logo.png" alt="" class="nav-logo-img" onerror="this.style.display='none';this.nextElementSibling.style.display='flex';">
                <div class="logo-icon" aria-hidden="true" style="display:none;"><%= (process.env.SITE_INITIALS || siteName.substring(0,2).toUpperCase()) %></div>
                <span class="nav-brand-text"><%= siteName %></span>
            </a>

            <% if (!user) { %>
            <a href="/auth/login" class="nav-login-mobile btn btn-outline btn-sm">Log In</a>
            <% } %>

            <ul class="nav-links" id="nav-links">
                <li><a href="/" class="<%= currentPath === '/' ? 'active' : '' %>">Home</a></li>
                <li><a href="/about" class="<%= currentPath === '/about' ? 'active' : '' %>">About Us</a></li>
                <li><a href="/services" class="<%= currentPath === '/services' ? 'active' : '' %>">Services</a></li>
                <li><a href="/tutors" class="<%= currentPath.startsWith('/tutors') ? 'active' : '' %>">Our Tutors</a></li>
                <li><a href="/blog" class="<%= currentPath.startsWith('/blog') ? 'active' : '' %>">Blog</a></li>
                <li><a href="/contact" class="<%= currentPath === '/contact' ? 'active' : '' %>">Contact</a></li>
                <% if (user) { %>
                    <% if (user.role === 'owner') { %>
                        <li><a href="/admin/owner" class="btn btn-primary btn-sm" style="margin-left:8px;">Dashboard</a></li>
                    <% } else if (user.role === 'tutor') { %>
                        <li><a href="/admin/tutor" class="btn btn-primary btn-sm" style="margin-left:8px;">Dashboard</a></li>
                    <% } else { %>
                        <li><a href="/parent/dashboard" class="btn btn-primary btn-sm" style="margin-left:8px;">Dashboard</a></li>
                    <% } %>
                    <li class="nav-auth-item"><span class="nav-greeting">Hi, <%= user.firstName %>!</span></li>
                    <li class="nav-auth-item"><a href="/auth/logout" class="btn btn-outline btn-sm">Log Out</a></li>
                <% } else { %>
                    <li class="nav-auth-item"><a href="/auth/login" class="btn btn-outline btn-sm">Log In</a></li>
                    <li class="nav-auth-item"><a href="/consultation" class="btn btn-secondary btn-sm">Free Consultation</a></li>
                <% } %>
            </ul>

            <button class="nav-hamburger" id="nav-hamburger" aria-label="Toggle navigation menu" aria-expanded="false" aria-controls="nav-links">
                <span></span><span></span><span></span>
            </button>
        </div>
    </nav>

    <% if (typeof success !== 'undefined' && success) { %>
    <div class="alert alert-success" role="alert" style="border-radius:0;margin:0;"><span aria-hidden="true"></span> <%= success %></div>
    <% } %>
    <% if (typeof error !== 'undefined' && error) { %>
    <div class="alert alert-error" role="alert" style="border-radius:0;margin:0;"><span aria-hidden="true"><%= error %>#x26A0;<%= error %>#xFE0F;</span> <%= error %> <% if (typeof resendEmail !== 'undefined' && resendEmail) { %> <a href="/auth/resend-verification?email=<%= encodeURIComponent(resendEmail) %>">Click here to resend</a><% } %></div>
    <% } %>
